ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional — omit to skip verified contact lookup (leads will only show
# whatever contact info Claude happened to find during research, unverified).
HUNTER_API_KEY=your-hunter-io-key-here
